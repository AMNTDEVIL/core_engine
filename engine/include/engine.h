#ifndef MAIN_ENGINE_H//if not define then define in next line
#define MAIN_ENGINE_H
#include "../engine_config.h"
#include "../engine_version.h"

    int engine_init(void);//initialize the engine
    int engine_shutdown(void); //shutdown the engine
#endif //stop ENGINE.h